<%# Note layout: title, author, date. Mirrors _layouts/note.md.
     Used by research notes (e.g. Achilles' Tent series). %>
<h2>
    <%= @page[:title] %>
    <small>
        written by Nathan Lilienthal
        on <%= format_date(@page[:published_at]) %>.
    </small>
</h2>

<%= @content %>
